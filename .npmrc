# Supply-chain mitigation: quarantine packages published less than 7 days
# ago, reducing exposure to compromised versions from npm supply-chain
# attacks (e.g. Shai-Hulud-style worms).
# Honored by npm v11+; silently ignored on older npm versions.
min-release-age=7
